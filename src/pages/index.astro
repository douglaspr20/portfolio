---
import { languages } from "@/i18n";


const currentLang = Astro.currentLocale ?? 'en';

const redirectTo = (currentLang in languages) ? currentLang : "en";

return Astro.redirect(`/${redirectTo}`);

---
